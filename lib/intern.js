const Employee = require('./employee');

class Intern extends Employee {
    constructor() {
        super(employeeName, id, email, github, office)

        this.getRole() = role;
        this.getSchool() = school;
    }
}